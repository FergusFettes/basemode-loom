# basemode-loom

Persistent branching exploration for `basemode` continuations.

## Install

```bash
pip install basemode-loom
```

`basemode-loom` depends on `basemode` from PyPI as a normal package dependency.

## CLI

```bash
basemode-loom --help
```

