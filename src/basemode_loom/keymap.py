# KeyMap has moved to config.py. Re-exported here for backward compatibility.
from .config import DEFAULT_CONFIG as DEFAULT_CONFIG
from .config import KeyMap as KeyMap

DEFAULT_KEYMAP = DEFAULT_CONFIG.keys
